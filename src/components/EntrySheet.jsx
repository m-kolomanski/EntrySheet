import React from 'react';
import { useState } from 'react';
import PropTypes from 'prop-types';
import '../styles/EntrySheet.css';

export const EntrySheet = ({id, data, history, layout, pick_lists, field_types, box_data, field_buttons, editable, more_options}) => {
    return (
      <div className="entry-sheet" id={id}>HELLO</div>
    );
};

EntrySheet.propTypes = {

};
  
EntrySheet.defaultProps = {

};